{% extends 'base.html.twig' %}

{% block title 'gerer les locations' %}

{% block body %}

<div class="row">
    <div class="col-md-6 mx-auto">
        <h1 class="text-center text-muted mb-4 mt-5">modification d'annonce</h1>

        {{form_start(form)}}

        {{form_widget(form)}}

        <input type="submit" value="modifier" class="btn btn-secondary btn-block" formnovalidate>

        {{form_end(form)}}
    </div>
   </div>
{% endblock %}
